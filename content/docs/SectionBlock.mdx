---
title: SectionBlock
description: A padded, optionally styled wrapper for vertically stacked content blocks.
---

import SectionBlock from "@/components/layout/SectionBlock";
import { Divider } from "@/components/mdb/components/docs/Divider";
import CodeBlockWrapper from "@/components/mdb/components/docs/CodeBlockWrapper";

# `SectionBlock`

A vertical layout container with built-in spacing and padding — useful for separating large page blocks.

<Divider />

## 🚀 Basic Usage

<CodeBlockWrapper language="tsx">
  ```tsx
  <SectionBlock>
    <h2 className="text-2xl font-bold">Stacked Section</h2>
    <p className="text-muted">Helps keep consistent vertical rhythm throughout your UI.</p>
  </SectionBlock>
---
title: SectionBlock
description: A padded, optionally styled wrapper for vertically stacked content blocks.
---

import SectionBlock from "@/components/layout/SectionBlock";
import { Divider } from "@/components/mdb/components/docs/Divider";
import CodeBlockWrapper from "@/components/mdb/components/docs/CodeBlockWrapper";

# `SectionBlock`

A vertical layout container with built-in spacing and padding — useful for separating large page blocks.

<Divider />

## 🚀 Basic Usage

<CodeBlockWrapper language="tsx">
  ```tsx
  <SectionBlock>
    <h2 className="text-2xl font-bold">Stacked Section</h2>
    <p className="text-muted">Helps keep consistent vertical rhythm throughout your UI.</p>
  </SectionBlock>

```

</CodeBlockWrapper>
```
