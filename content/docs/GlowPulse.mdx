---
title: GlowPulse
description: A pulsing glow effect used to draw attention to a target element.
---

import GlowPulse from "@/components/overlays/GlowPulse";
import { Divider } from "@/components/mdb/components/docs/Divider";
import CodeBlockWrapper from "@/components/mdb/components/docs/CodeBlockWrapper";

# `GlowPulse`

Adds an animated glow behind or around elements — useful for emphasis or onboarding highlights.

<Divider />

## 🚀 Basic Usage

<CodeBlockWrapper language="tsx">
  ```tsx
  <GlowPulse>
    <button className="btn">Try It Now</button>
  </GlowPulse>

```

</CodeBlockWrapper>

<Divider />
```
